# Known Bugs

Bugs noticed during testing that aren't immediate blockers. They get fixed when Step priorities allow.

| ID | Noticed | Severity | Component | Description |
|---|---|---|---|---|
| BUG-001 | 2026-05-06 | Low | Search | Search field doesn't handle Swedish characters (å, ä, ö). Typing without diacritics doesn't match stops whose names contain them, and vice versa. Need locale-aware normalisation (e.g. `Collator` / NFD + diacritic strip) so `"angerm"` matches `"Ångermanland"`. |
| BUG-002 | 2026-05-06 | **Fixed 2026-05-07 (runtime confirmed)** | Map / markers | When the map is zoomed out, the "400 nearest to center" markers all clump at the center while the rest of the visible map has no markers — looks unnatural. Possible fix: only render markers above a zoom threshold (e.g. zoom ≥ 12), or switch to true clustering (osmdroid-bonuspack `RadiusMarkerClusterer`) that shows summary icons when zoomed out and individual stops when zoomed in. **Fix shipped 2026-05-07:** chose the zoom-threshold path (lower complexity than full clustering). `MIN_STOP_ZOOM = 14.0` constant in `MainActivity`; `rebuildVisibleMarkers` skips marker construction below this level. Initial map zoom is 13, so user must zoom in once before stops appear — also addresses the "too distracting on first launch" UX feedback that drove this fix. |
| BUG-003 | 2026-05-06 | **Fixed 2026-05-07 (runtime confirmed)** | Map / markers | Default OSMDroid stop marker icon is too large when zoomed out — markers visually overlap and obscure the map tiles. Use a smaller custom icon (small dot / drawable) and possibly scale with zoom level. **Fix shipped 2026-05-07:** stop markers now use a 6 dp filled circle (`STOP_DOT_DIAMETER_DP = 6f`, `STOP_DOT_COLOR = 0xFF607D8B` muted blue-grey) instead of the default OSMDroid pin. `makeStopDot` was extended to accept a size parameter so commute-overlay stops keep the existing 12 dp size. Anchor changed from `ANCHOR_BOTTOM` to `ANCHOR_CENTER` to match the symmetric dot. |
| BUG-004 | 2026-05-07 | Pre-distribution blocker | Map / tile provider | App currently uses OSMDroid's default `TileSourceFactory.MAPNIK` which fetches from `tile.openstreetmap.org`. The OpenStreetMap Foundation's tile-usage policy prohibits this for distributed apps regardless of commercial intent (volume-based, not status-based). Must switch to a third-party tile provider (CartoDB / MapTiler / Mapbox / OpenFreeMap / similar) before any wider distribution — including free/non-commercial release. Fine to leave for development phase. Tracks NFR9. |
| BUG-005 | 2026-05-07 | **Fixed 2026-05-07 (runtime confirmed)** | Step 4 / direction labels | The bundled `sl-lines.json` produced by `extractGtfs` had empty `headsign` strings on some directions because SL's GTFS feed leaves `trip_headsign` blank for those trips (notably bus 3 in both directions). Caused empty direction labels AND, surfaced in Step 5, prevented direction-correct filtering of live vehicles. **Fix shipped 2026-05-07:** in `extractGtfs`, when `trip_headsign` is blank, fall back to the name of the trip's final stop. Re-ran extractor, regenerated `sl-lines.json`. **Runtime confirmed 2026-05-07:** Logcat showed populated headsigns ('Motalavägen', 'Tengdahlsgatan') instead of empty strings; matchDirection picked correct direction once paired with BUG-009 fix. |
| BUG-006 | 2026-05-07 | Low | Performance / UI jank | Bottom sheet open / close and saving a commute trigger 1.4–1.8 s frame times on the emulator (Logcat "Davey!" warnings + one "Skipped 93 frames"). Caused by main-thread work — likely a combination of OSMDroid rebuilding 400 stop markers on map idle, bottom sheet inflating Material Design components synchronously, and the polyline + stop-dot overlays being added in one shot. Possible fixes: pre-inflate the bottom sheet on activity start, throttle marker rebuilds harder, batch-add overlays on a worker, profile with Android Studio's CPU/Memory profilers to confirm the dominant cost. User flagged as low priority — visual polish, not a blocker. |
| BUG-007 | 2026-05-07 | Low (feature) | Commute config / quota | Cap each saved commute's active time window to a maximum of 30 minutes. Enforced in the bottom-sheet config UI (start/end picker validation). Rationale: protects the Trafiklab Bronze monthly quota even if a user saves a wildly long window by mistake; also matches realistic commute durations. Defer until quota usage is actually observed in practice. |
| BUG-008 | 2026-05-07 | Low | Map / overlays | When a newly-saved commute's polyline overlaps a previous commute on the map, the new one isn't visually drawn (or appears identical to the existing one). Suspected causes: same color picked from `COMMUTE_PALETTE` (only 5 colours, cycle by index), z-order making the older line render on top, or the overlapping polyline literally being identical-looking under stroke alpha 180. Investigation needed: log polyline endpoints + colours when saving, render z-order, possibly bump newer overlays up the overlay stack. Reported during Step 5 testing. |
| BUG-009 | 2026-05-07 | **Fixed 2026-05-07 (runtime confirmed, three-version fix arc)** | Step 5 / direction matching | SL Transport API's `direction` string (e.g. "Sofia" — shown on the bus's destination sign at the user's stop) doesn't always match the GTFS final-stop name (e.g. "Motalavägen" — the literal end of the route). **Fix v1 (2026-05-07):** added `directionCode: Int?` to `CommuteConfig`, saved from `StopLineOption.directionCode`. matchDirection preferred direction_id match. **Fix v2 (2026-05-07):** runtime test showed SL uses 1-based codes (config had directionCode=2, GTFS direction_id is 0-based) — added `code-1` heuristic fallback. **Fix v3 (2026-05-07):** consulted Trafiklab OpenAPI spec on user request — confirmed `direction_code` is 0/1/2 with 0 = unidentified, and Trafiklab support explicitly states SL Transport and GTFS Realtime systems "don't share IDs" and recommends matching via trip_id and stop_times. Replaced heuristic as primary with **stop-sequence-aware matching**: added `stopName: String?` to `CommuteConfig`, saved from bottom sheet, matchDirection finds the GTFS direction where `config.direction` appears AFTER `config.stopName` in the stop ordering. **Runtime confirmed 2026-05-07:** with stopName='Tullgårdsparken' populated and config.direction='Sofia', matchDirection returned direction_id=1 (Tengdahlsgatan) — the documentation-aligned answer, which also happens to be what the heuristic gave us. Both algorithms agree. Three buses correctly tracked toward Sofia. |
| BUG-010 | 2026-05-07 | **Fixed 2026-05-07 (runtime confirmed)** | Step 6 / vehicle marker InfoWindow | Tapping a vehicle marker shows the InfoWindow popup, but it can't be dismissed by tapping the map (no close affordance). The default OSMDroid behaviour SHOULD close it on map tap, but apparently doesn't trigger reliably in our setup. **Fix shipped 2026-05-07:** added a `MapEventsOverlay` at index 0 in the map overlay stack whose `singleTapConfirmedHelper` calls `InfoWindow.closeAllInfoWindowsOn(map)`. Returns `false` so the event continues to other overlays — only fires when no marker was tapped. |
| BUG-011 | 2026-05-07 | **Fixed 2026-05-07 (runtime confirmed)** | Map / zoom controls | OSMDroid's built-in zoom +/- buttons auto-hide when not in use AND are anchored at the bottom-center of the map, where they overlap the live_status banner. **Fix shipped 2026-05-07:** disabled the built-in controller (`map.zoomController.setVisibility(NEVER)`) and added two custom Buttons (`@id/zoom_in`, `@id/zoom_out`) anchored bottom-end with a 72 dp bottom margin to clear the live_status banner. Always visible. Wired to `map.controller.zoomIn()` / `zoomOut()`. The live_status banner itself is now hidden when state is `Idle` (was previously a 24 dp empty bar at the bottom even before tracking started). |
| BUG-012 | 2026-05-07 | Low — investigate later | Step 6 / vehicle quality | Vehicle markers frequently render as `UNCERTAIN` (greyed, "uncertain" tag in title) even when the polling cycle has just succeeded. Suspected cause: SL's GTFS-RT feed populates `vehicle.timestamp` with the bus's last GPS report time, which can lag the feed's emit time by tens of seconds. Our `STALE_THRESHOLD_MS = 60_000` may be too tight for this feed. Need empirical observation: log the distribution of `nowMs - tsMs` over a few hours of normal SL service to pick a sensible threshold (likely 90–120 s), or weight LIVE-vs-UNCERTAIN by feed-emit time when available. Defer until Step 7 (disruptions) or Step 10 (polish) — doesn't affect tracking correctness, only the visual confidence indicator. |
| BUG-013 | 2026-05-08 | Low (feature / refinement) | Notifications / commute alerts | User-defined GPS-trigger waypoint for commute notifications. User picks a point along the route polyline (or drops a custom point near the route); when the live GPS position of the tracked vehicle passes that point in the user's travel direction, the app fires a one-shot push notification ("your bus has reached <waypoint>" / "leave now"). Lets the user calibrate "when to leave home" to their own walking time to the stop, rather than relying on a fixed `eta <= 3` heuristic. Filed instead of the LeaveNow notification suggested in the design handoff — user explicitly preferred a configurable, user-controlled waypoint over a fixed-distance trigger. Defer until Phase 1 widget + foreground service ship; this is a polish/refinement on top of Step 8 (uses the same foreground-service tick that updates the widget; the trigger evaluation is a per-poll geometric check on the tracked vehicle's lat/lon vs. the saved waypoint). Considered for Step 10 polish or Phase 2 if scope expands. |
| BUG-014 | 2026-05-08 | **Fixed 2026-05-08 (runtime confirmed)** | Step 8b / widget visual scope | Widget initially showed the FULL route from line origin (Motalavägen) to terminus (Tengdahlsgatan) with the user's stop in the middle. User UX feedback: this is decision-support for catching a bus at a specific stop, not a journey planner — anything past the user's stop is out of scope per `project_app_scope.md`, and showing the entire route makes it harder to visually parse "is the bus close to my stop yet". **Fix shipped 2026-05-08:** windowed the route gauge to **at most 5 stops** ending at the user's stop (rightmost ringed dot). State refactored: removed `stopCount` / `userStopIndex` / `busIndex` / `firstStopName` / `lastStopName` from `WidgetCommuteState`, added `visibleStopCount` (1..5), `visibleBusIndex` (window-local Float?, null when out of window), `stopsAwayFromUser` (Int?, ceil-rounded conservative), `visibleStartStopName`. Bus past user's stop yields null + null because Phase.Passed takes over. **Runtime confirmed:** screenshot showed bus 57 mid-gauge, ★ Nätgränd rightmost, Grindsgatan as leftmost, no post-stop labels. |
| BUG-015 | 2026-05-08 | **Fixed 2026-05-08 (runtime confirmed)** | Step 8b / widget header right slot | Widget header right slot was empty; design handoff specified scheduled departure time should appear there in mono font. User explicitly asked: "what the 'supposed' arrival time of the schedule is, if you can find this in the API? It will have a scheduled time and then our estimated arrival based on if its late or not." **Fix shipped 2026-05-08 (sub-step 2.6):** added `scheduledClockTime: String?` and `estimatedClockTime: String?` to `WidgetCommuteState`, populated by deriver from `nextDeparture.scheduledTime` / `estimatedTime` formatted via `DateTimeFormatter.ofPattern("HH:mm")`. Estimated only shown when it differs from scheduled (no clutter). Renderer formats header right slot as "07:54" (on schedule) or "07:54 → 07:56" (with predicted delay). **Runtime confirmed:** screenshot showed "18:49" in header right after re-test. |
| BUG-016 | 2026-05-08 | **Fixed 2026-05-08 (three-version arc, runtime confirmed v3)** | Step 8b / widget GPS data freshness | Widget had no indicator of how fresh the live GPS data is. User asked: "im missing some feedback on it, like when the gps was last updated etc." **Fix v1 (sub-step 2.6):** added `vehicleAgeSeconds: Int?` to `WidgetCommuteState` sourced from the locked vehicle's GTFS-RT `vehicle.timestamp` (NOT poll time — per BUG-012 SL's feed often lags actual GPS by 30-90s). Suffix appended to delta label: "on time · 12s". **Fix v2 (sub-step 2.7):** user said the suffix was confusing ("hard to understand what that second timer is for") and was sometimes stuck at "0s". Two underlying fixes: (a) Repository-level — `GtfsRealtimeRepository` was falling back to `nowMs` when SL omitted `vehicle.timestamp`, making age incorrectly read 0s. Changed to leave `timestampMs = 0L` as a sentinel for "unknown" (quality calculation preserved via separate `qualityBasisMs` so existing LIVE/UNCERTAIN visualisation in Step 6 doesn't change). Deriver suppresses `vehicleAgeSeconds` when timestampMs is 0L. (b) Layout-level — moved age to its own dedicated TextView (`widget_gps_age`) below the bottom row, with descriptive text "Bus position · 12s ago", visibility GONE when GPS time unknown. **Fix v3 (sub-step 2.8, runtime-confirmed by user with new screenshot):** even after v2, user saw "0s ago" frozen between widget pushes — static text doesn't tick during the 20-s gap between foreground-service refreshes, so the displayed value sat at "0s" for 20 s, then snapped back to "0s" on next poll. Switched the GPS-age view from `TextView` to `Chronometer`, which auto-ticks every second inside the launcher's process without us having to push widget updates. `WidgetCommuteState.vehicleAgeSeconds: Int?` replaced with `vehicleTimestampMs: Long?` (raw GPS epoch ms, null when unknown). Renderer computes `elapsedBase = SystemClock.elapsedRealtime() - (System.currentTimeMillis() - tsEpoch)` and calls `RemoteViews.setChronometer(viewId, elapsedBase, "Updated %s ago", started=true)`. Format `%s` gets replaced by the Chronometer with elapsed time as `MM:SS` (e.g. "Updated 00:12 ago" → ticks to "Updated 00:13 ago" → ...). When the next service push (~20 s later) sets a new base from a fresh GPS timestamp, Chronometer resets to "0:00" and counts up again — same auto-ticking behaviour, just with a fresh anchor. |
| BUG-017 | 2026-05-08 | **Fixed 2026-05-08 (runtime confirmation pending)** | Step 8b / widget off-gauge indicator | Sub-step 2.5's off-gauge indicator was a chunky rounded-rect chip baked into the route bitmap reading "57 ←10" in the bus's phase color (red for Late/Deviation). User feedback: "Wheh its further away than our 5 stops, its indicated by just a number example: '57 - 10' this is hard to understand and its also in the 'red' label same as 57, instead put that 'currently 10 stops away' in its own text, preferably under, and just some symbol with 57> or something." **Fix shipped 2026-05-08 (sub-step 2.7):** dropped the `drawStopsAwayChip` helper from `WidgetBitmapRenderer` entirely (route gauge now stays clean — empty when bus is out of window, drawn when bus is in). Added a dedicated TextView (`widget_stops_away`) below the stop labels row with descriptive text "Bus is N stops away" (or "1 stop away" for singular), amber accent color (`#FFB74D`) so it stands out without alarming red. Visible only when `stopsAwayFromUser != null`. Removed the now-unused `RectF` import. |
| BUG-018 | 2026-05-08 | **Fixed 2026-05-08 (runtime confirmation pending)** | Step 8b / widget hero ETA caption | Hero ETA number "8 min" had no descriptive label, leaving the user without explicit context for what the number represents. User: "And the '4 min' is also without explanation. Put something more, like '~estimated arrival: ' or something." **Fix shipped 2026-05-08 (sub-step 2.7):** added a dedicated caption TextView (`widget_hero_label`) above the hero ETA with text "Estimated arrival" (10sp grey), inside a vertical-stacked sub-LinearLayout in the bottom row's left side. Caption hidden in Dormant / Passed phases since the hero text ("—" / "passed") is self-explanatory in those cases. |
| BUG-019 | 2026-05-08 | **Fixed 2026-05-08 (runtime confirmation pending)** | Step 8b / widget light-mode support | Widget originally rendered with `?android:attr/colorBackground` and theme-attr text colors, picking up the system theme automatically. User decided: "the widget.. maybe just have one state only, only dark mode. Light mode can be for later, add it somewhere as low priority." **Fix shipped 2026-05-08 (sub-step 2.7):** all colours hardcoded for dark mode in `widget_commute.xml` — background `#1E1E1E` (very dark grey), primary text `#FFFFFF` (hero ETA, user stop label), secondary text `#9E9E9E` (scheduled, stop labels, captions, GPS age), destination `#E0E0E0`, stops-away amber `#FFB74D`. Line badge stays blue `#1976D2`, deviation pill stays amber `#FFB300` with black text. Bitmap renderer colours unchanged (designed for either mode). Light-mode support **filed as deferred refinement** under BUG-020. |
| BUG-020 | 2026-05-08 | Low (refinement / deferred) | Step 8b / widget light-mode support | Widget is dark-mode only; system light/dark setting is ignored. Per user decision (see BUG-019), this is acceptable for Phase 1 and should be revisited as a polish item. **Path forward:** introduce a `night/widget_commute.xml` variant + a `day/widget_commute.xml` variant with theme-aware color attributes, OR pull colors from a `colors.xml` palette with `night-` qualifiers. Bitmap renderer colours may need parallel adjustments (currently grey route line + blue user-stop dot read fine on either mode but should be verified). Defer to Step 10 polish. |
| BUG-021 | 2026-05-08 | **Fixed 2026-05-09 (runtime confirmation pending, two-version arc)** | Step 8b / widget stops-away wording | User saw "Bus is 14 stops away" alongside "Estimated arrival 9 min" and asked: "how can the bus be 14 stops away and that is 8 min estimated arrival time? that sound wayyy to little." The data is actually consistent (line 57 inner-city Stockholm has stops ~200-400m apart, so 14 stops ≈ 4.2km ≈ ~9-10 min at urban bus speeds), but the original wording "stops away" was suspected of priming the user to expect minute-equivalent intuition. Compounding factor: the stops-away count comes from GTFS-RT VehiclePositions (live bus lat/lons) while the ETA comes from SL Departures (their prediction model) — Trafiklab support has confirmed `journey.id` ≠ GTFS-RT `trip_id` (anti-BUG-009 docstring on `StopRepository.getNextDeparture`), so the bus we visualise on the gauge isn't necessarily the same physical bus SL is predicting will arrive at the user's stop. **Fix v1 (sub-step 2.9, 2026-05-08):** rephrased "Bus is N stops away" → "Bus is N stops behind". **Fix v2 (sub-step 2.10, 2026-05-09):** user reviewed the new wording on a fresh screenshot ("Bus is 7 stops behind" alongside "Estimated arrival 10 min", math now plausible at first glance: 7 × ~85s ≈ 10 min) and reported that "behind" reads worse than "away" in this UI context — preferred a tighter "N stops away" without the "Bus is" prefix (the route gauge above already supplies the visual context for *which* bus). Reverted to **"$N stops away"** / "1 stop away". Hypothesis: the original "14 stops vs 8 min" mismatch was an outlier driven by line 57 inner-city density, NOT a generic wording issue — for typical numbers the user's intuition tracks the data fine, and "away" reads more natural than "behind". Two-data-stream caveat preserved in WidgetRenderer code comment for future maintainers. **Future work (deferred):** if the bus-position vs ETA mismatch confuses users repeatedly, consider linking the two streams by matching SL's `journey.id` schedule time to a GTFS-RT trip's known scheduled stop_time. Complex (requires loading stop_times.txt slices into the app); defer until evidence of recurring confusion. |
| BUG-022 | 2026-05-09 | **Fixed 2026-05-09 (runtime confirmation pending)** | Step 8b / widget bus marker clipping | The bus marker circle (busRadius 11dp + 1.5dp outline = 12.5dp from center) was being clipped at top and bottom of the route gauge bitmap because the route ImageView was only 22dp tall (midY = 11dp). Marker top edge = 11dp − 12.5dp = −1.5dp clipped; marker bottom = 11dp + 12.5dp = 23.5dp clipped. Visual artefact made the bus marker look squashed / cut off. **Fix shipped 2026-05-09 (sub-step 2.11):** bumped `ROUTE_LINE_DP_HEIGHT` from 22 to 28 in `WidgetRenderer.kt` and matching `widget_route` ImageView height in `widget_commute.xml`. Now midY = 14dp, marker top = 1.5dp, marker bottom = 26.5dp — full marker fits with ~1.5dp breathing room each side. Net widget vertical impact = +6dp; comfortable within the 2-cell-tall slot the user already resized into. |
| BUG-023 | 2026-05-09 | **Fixed 2026-05-09 (runtime confirmation pending, two-version arc)** | Step 8b / widget header times legibility | Header right slot showed "00:49 → 00:50" (scheduled clock → estimated clock at user's stop) with no caption. User: "And the top right, what are those numbers? And what is the logic behind estimated arrival 19 min? The numbers seem wrong and idk what they are based on." Diagnosis: the numbers are mathematically consistent — 00:50 (estimated) − 00:31 (now at screenshot capture) = 19 min hero countdown; 00:50 − 00:49 = +1' late delta. **All three (top-right times, hero ETA countdown, delta) encoded the same single fact (predicted bus arrival) in three formats.** That redundancy made the widget hard to read at a glance: the user couldn't tell which number was authoritative or how they related. **Fix v1 (sub-step 2.11, 2026-05-09):** dropped the estimated clock time from the header right slot entirely, and prefixed the remaining scheduled time with a "Sched" caption: format changed from `"00:49 → 00:50"` → `"Sched 00:49"`. **Fix v2 (sub-step 2.12, 2026-05-09):** user reviewed v1 wording: "first off, what the heck is Sched ?! how is that clear? maybe 'per timetable:' but i think thats too long to put there. maybe just the number then since it wont be super clear anyways. or move it down so its over the scale showing how late/early the bus is - so over it it says something like 'scheduled arrival: 00:49 -> *new time*'". Acted on user's preferred option (the "move down" one): removed the header right slot entirely, restructured bottom row's right side into a vertical stack of `widget_scheduled_label` (new id) + `(scale gauge + delta)`. Caption format: `"Scheduled arrival: 00:49 → 00:50"` when prediction differs from timetable, `"Scheduled arrival: 00:49"` when on schedule. Now the schedule-conformance story (timetable → predicted → conformance gauge → delta) reads as one connected block. Restored both clock times for direct readability — user can now see the absolute predicted clock time again without having to compute "now + N min". `widget_scheduled` TextView removed from layout entirely. Also: changed delta format `"+5' late"` / `"-2' early"` → `"+5 min late"` / `"2 min early"` (drops prime symbol, which is technically the arc-minute glyph and wrong for time-minutes; uses absolute value for early since the "early" word already carries the direction). |
| BUG-024 | 2026-05-09 | Medium — primary use case affected | Commute config / line picker | When the user opens the commute-config bottom sheet for a stop at off-hours (e.g. late at night), the line picker is empty or missing lines that don't currently have running buses — even though those lines DO serve the stop in service hours. Concrete example reported by user: at ~00:35 on 2026-05-09, the user tried to save a commute for **bus 57 at Nätgränd** (a morning commute they want to set up the night before) but **line 57 was not visible** in the line list because no 57 buses are running that late. **Underlying cause (hypothesis, not yet confirmed):** the line picker is most likely populated from the SL Transport Departures API (live upcoming departures within a window) rather than from the bundled GTFS-derived `sl-lines.json` static catalog (which has all lines that serve each stop in their normal service patterns regardless of current time). When Departures returns 0 results for an off-service line, the line is invisible. **User workflow impact:** prevents the canonical "save tomorrow's commute the night before" use case, which is core to the app's purpose per `project_app_scope.md` (pre-trip commute decision support). **Fix direction (when picked up):** source the line list at the line-picker step from the static catalog (`sl-lines.json` already has stop→lines mapping from GTFS-extracted data), not from a live-departures call. Live departures should still drive the *active commute tracking* path; only the *config-time* line picker should fall back to static. Verify which file does the line-picker query (probably the bottom-sheet code added around Step 4/5) and which data source it currently uses. **Defer to:** Step 9 if it requires a non-trivial refactor; Step 8b is closing out and this isn't a widget-surface bug. |
