# Project Plan

**Project:** SL Transit Android App
**Thesis context:** Artifact for DSR study on AI-assisted development by a non-programmer
**Article goal:** Document the full Claude-assisted build process
**Started:** 2026-05-04
**Collaborators:** 2 people (both on Windows)

---

## Current Tech Stack

| Decision | Choice | Reason |
|---|---|---|
| Platform | Android native | Lock screen widget required; iOS blocked on Windows |
| Language | Kotlin | Modern Android standard |
| IDE | Android Studio | Free, Windows, includes SDK + emulator |
| Lock screen feature | Android App Widget | Native widget, updates live, works on lock screen |
| Live data | Trafiklab API (SL) | Free, open, covers Stockholm public transport |
| APIs needed | SL Transport + SL Deviations + SL Journey Planner + GTFS Regional Realtime | Departures/delays, deviations, journey planning, live vehicle positions |
| Map library | OSMDroid (OpenStreetMap) | Free, no API key, native Android library |
| Version control | Git + GitHub | Backed up, collaborative, good for article |
| Repo visibility | Public | Good for thesis/article |

---

## Phases

### Phase 0 — Setup
> Goal: everything in place before writing a single line of app code

- [x] Both: install Android Studio (studio.android.com)
- [x] Both: register at trafiklab.se — APIs used:
  - SL Transport, SL Deviations, SL Journey Planner (no key needed)
  - GTFS Regional Realtime (key obtained)
- [x] Person A: create GitHub repo
  - https://github.com/Ronja01010101/Thesis-project-Vibe-Coding-a-mobile-app
- [x] Person A: add Person B as collaborator (Settings → Collaborators)
- [x] Both: clone the repo locally
- [x] Create new Android project in Android Studio (Kotlin, Empty Activity)
- [x] Verify .gitignore is present (Android Studio generates this)
- [x] Add API keys to local.properties — GTFS Realtime key added; SL APIs need no key
- [x] Create CLAUDE.md in repo root
- [x] Move USAGE_LOG.md and PLAN.md into repo
- [x] First commit + push: "project setup"

### Phase 1 — Requirements
> Goal: a clear, prioritized build list before any coding starts

- [x] Share requirements list
- [x] Structure and prioritize requirements together
- [x] Create REQUIREMENTS.md in repo
- [x] Break requirements into buildable chunks with order

### Phase 2 — Build
> Goal: build one requirement at a time, fully logged

- [ ] One requirement at a time
- [ ] Feature branch per requirement
- [ ] Merge to main when requirement is done and tested
- [ ] Usage log updated every exchange

### Phase 3 — Wrap Up
> Goal: reflection and article material

- [ ] Run `/cost` for final token summary
- [ ] Analyze USAGE_LOG.md — patterns, difficult requirements, prompt counts
- [ ] Reflect on process for thesis/article

---

## Collaboration Rules

- One person drives Claude sessions per feature — no parallel editing
- Always `git pull` before starting work
- Branch per requirement → merge to main when done
- Decide who is lead developer per session before starting

---

## Change Log
> All plan changes recorded here so nothing is lost

| Date | Change | Reason |
|---|---|---|
| 2026-05-04 | Initial plan created | First planning session |
| 2026-05-04 | Tech stack changed from React + Vite (web) → React Native | Lock screen widget requirement surfaced |
| 2026-05-04 | Tech stack changed from React Native → Android native (Kotlin) | True lock screen widget requires native; iOS blocked on Windows; Android-only accepted |
| 2026-05-05 | Map library changed from Google Maps SDK → OSMDroid (OpenStreetMap) | Free, no billing setup, no API key needed, native Android library |
| 2026-05-05 | API set finalised: SL Transport + SL Deviations + SL Journey Planner + GTFS Regional Realtime | SL Transport covers departures; GTFS Regional Realtime covers live vehicle positions; Deviations and Journey Planner cover Phase 2 reqs |
| 2026-05-06 | Step 3 (P1-FR2) implemented on branch `step-3-commute-config`, merged to `main` after build verification | Marker tap or stop search opens a bottom sheet; user picks line+direction (loaded from SL departures), start/end time; saved to SharedPreferences with overlap rejection. Runtime test pending — collaborator to execute the smoke-test plan. |
| 2026-05-06 | **Emergency fix:** new step "Step 3.5 — Limit map data" inserted into build order; new NFR8 added | Both collaborators' machines pegged at 100% CPU/RAM and could not start the emulator. Suspected cause: ~14k stop markers being rendered in OSMDroid simultaneously. Fix is viewport-bound rendering with a hard cap, rebuilt on map idle. Branch `step-3-5-limit-map-data`. |
| 2026-05-06 | Step 3.5 implementation finalised and merged to main | Initial bbox-based filter wasn't reliable on a slow emulator (boundingBox returned a degenerate box pre-layout). Replaced with "render 400 stops nearest to map center" — same hard cap, same pan-rebuild behaviour, but no dependence on MapView projection state. Confirmed working in emulator after a network/VPN issue was resolved on the host. Two cosmetic follow-ups parked in BUGS.md (BUG-002 cluster-at-center, BUG-003 oversized marker icon). |
| 2026-05-07 | Step 4 split into 4a + 4b. Step 4a redesigned **before any code was written** from "download GTFS zip on the device + parse with OpenCSV + cache per-route as JSON" to "**pre-process GTFS zip on a developer machine at build time, ship a compact JSON inside `app/src/main/assets/`, app does no GTFS download or parsing**". Two new NFRs added (NFR9 tile-provider policy, NFR10 polling cadence). New Technical Constraint added for GTFS Static dataset size. New BUG-004 logged for the eventual tile-provider switch. | Reasoning chain (recorded in detail in USAGE_LOG entries 041–046 for thesis review): (1) The user committed to real route geometry (not straight stop-to-stop lines) because Step 5 ETA accuracy depends on it. (2) First research pass on Trafiklab GTFS Static recommended OpenCSV + SQLite/Room. I disagreed and proposed a simpler stream-parse + per-route JSON cache. (3) I asked the user three programmer-level design questions; the user (a non-programmer) couldn't evaluate them and asked me to research instead, providing five reference URLs (stranne SL.se spec, sl-map.gunnar.se, trafiklab.se, github.com/trafiklab/trafiklab.se, github.com/fltman/stockholms-puls). (4) Second research pass discovered that `sl-map.gunnar.se` ingests GTFS at build time, not on the device — a clean precedent that eliminates the on-device download (49 MB), the on-device CSV parse (30–90 s on slow hardware), and the SQLite/Room complexity. (5) Two additional findings from the research: raw OSM tile servers are forbidden for distributed apps regardless of commercial intent (NFR9), and Trafiklab Bronze realtime quota requires 10–15 s polling (NFR10). User accepted the redesign and explicitly asked for the decision-change to be logged in detail so a thesis reviewer can trace what triggered the plan change — flagged this as the first significant design reversal since the original tech-stack pivots. |
| 2026-05-07 | Step 4a implemented and merged to main on branch `step-4a-gtfs-extraction`. `extractGtfs` Gradle task working end-to-end. Output: `app/src/main/assets/sl-lines.json` (15 MB, 623 lines, 1166 directions, 21823 stops, 1165 polylines, lat/lon rounded to 5 decimals). | Implementation took four debug iterations (logged in USAGE_LOG entry 047): (a) Maven dependency download failed with SSL cert chain — fixed by adding `-Djavax.net.ssl.trustStoreType=Windows-ROOT` to gradle.properties, parallel to the earlier `git config http.sslBackend schannel` fix. (b) Kotlin .kts compiler crashed on locally-declared data classes inside the task lambda — fixed by hoisting `GtfsRoute` and `GtfsRepTrip` to top-level. (c) Trafiklab returned HTTP 406 — their endpoint requires `Accept-Encoding: gzip` even though the zip body is sent identity-encoded; added the header and a defensive gzip/deflate decoder. (d) First successful run produced a 58.8 MB JSON; reduced to 15 MB by dropping pretty-printing (each polyline coord was on its own indented line) and rounding coordinates to 5 decimals (~1 m accuracy, plenty for transit visualisation). One BUG-005 logged: empty `trip_headsign` on some lines means direction labels will display as empty strings — needs a fallback (e.g. final-stop name) before Step 4b ships. |
| 2026-05-07 | Step 4b implemented and merged to main on branch `step-4b-render-lines`. App now reads `sl-lines.json` at startup and draws each saved commute as a coloured polyline + filled-circle stop dots. Build verified clean; runtime test still pending the collaborator's emulator. | New `SlLine.kt` model + `SlLineRepository.kt` with async load and headsign-based direction matching. CommuteConfig extended with `lineDesignation` + `transportMode` (nullable for backward compat). Bottom sheet broadcasts `commute_saved` via `setFragmentResult` so MainActivity redraws overlays without waiting for onResume. Five-colour palette cycle distinguishes multiple commutes. Step 4b implementation was clean — no debug iterations beyond the initial write. Direction matching falls back through: exact headsign → contains either way → first direction (partial mitigation for BUG-005, full fix still on the BUGS.md backlog). |
| 2026-05-07 | Step 4b runtime-tested on emulator (user testing on own machine now, no longer collaborator-blocked). Initial run hit OOM-related instability — heap climbed to 135 MB peak during JSON parse, sustained 90–119 MB. Hotfix committed (`bac2809`): refactored `SlLineRepository` to stream-parse with Gson's `JsonReader`, keeping only entries matching saved `CommuteConfig.lineDesignation`. With 0 saved configs the asset isn't opened at all. Sustained heap dropped from ~10–15 MB → ~hundreds of KB; emulator-confirmed stable run after fix (heap stable at 5–17 MB). Step 4 (P1-FR3) is fully complete. UI jank during sheet interactions logged as BUG-006 (low priority). | **Methodological observation for thesis:** the build-verify pass alone (Step 4b first commit) wouldn't have caught the OOM — runtime testing on the target hardware was essential. The hotfix iteration cost ~$2–3 of token spend, small relative to the value of catching a feature that would have crashed on real devices. CLAUDE.md updated to expand the session-start reading rule from "USAGE_LOG only" to "PLAN.md, REQUIREMENTS.md, BUGS.md, USAGE_LOG.md (last 5–10 entries)" so future sessions pick up full context, not just the last few exchanges. |
| 2026-05-07 | Step 5 (P1-FR4 — Fetch live vehicle-position data) implemented and runtime-confirmed on emulator. Branch `step-5-live-data`. Approach: smoke-test the SL GTFS Realtime endpoint first (one-shot Gradle task, confirmed 100% trip_id reliability vs ~1% for route_id/direction_id, decisive for filter strategy), then implementation of `GtfsRealtimeRepository` + `LivePositionTracker` + `TrackingState` + bottom-screen verification overlay + commute-removal UI. Polling at 20s, foreground+active-window gated (NFR10). | **Two methodologically significant fix arcs along the way:** **(A) BUG-005 fix** — SL's GTFS leaves `trip_headsign` blank on many lines (notably bus 3, 17, 57). `extractGtfs` extended to fall back on the trip's final-stop name. Without this, direction-correct filtering was impossible because matchDirection had no headsign to compare against. **(B) BUG-009 three-version fix arc** — SL Transport's "direction" string ("Sofia") didn't match GTFS final-stop name ("Motalavägen") for line 57. v1: tried `direction_code` as GTFS `direction_id` — assumption was wrong (SL uses 1/2, GTFS uses 0/1). v2: added `code-1` heuristic — flipped to a different direction but unprincipled. v3: **after the user explicitly told me to consult Trafiklab's documentation**, found in their OpenAPI spec that `direction_code` is 0/1/2 (0 = unknown) and a Trafiklab support thread stating SL Transport and GTFS systems "don't share IDs", recommending reconciliation via trip_id and stop_times. Replaced heuristic with stop-sequence-aware matching: find the GTFS direction where the user's chosen direction-name appears AFTER their stop in the stop ordering. Documentation-aligned, robust against either system's labeling conventions. Required adding `stopName: String?` to CommuteConfig (because SL Transport `site.id` and GTFS `stop_id` use different schemas). Runtime confirmed: same answer as v2's heuristic (direction_id=1 for line 57 from Tullgårdsparken → Sofia → Tengdahlsgatan), but principled. **Two CLAUDE.md rules added** during the step: per-prompt USAGE_LOG discipline + step-transition `/cost` snapshots + manual-test outcome capture + decision-path documentation (added entry 065); always-consult-API-docs-before-guessing rule (added entry 073, citing BUG-009 as the load-bearing example). Cost: $30.90 over 3h 22m wall (Checkpoint 4) — substantially higher than prior steps, attributable to the 4-iteration BUG-009 arc and one full research-agent cycle that should have been the first thing done. **Thesis-relevant pattern documented across entries 053–074**: AI-assisted dev tends to drift toward plausible-looking heuristics absent explicit domain-source pressure; the user's "go read the docs" prompt was the corrective; documentation-first rules in CLAUDE.md are recommended as preventive scaffolding. |
| 2026-05-07 | Step 6 (P1-FR5 — Display live position of selected vehicle relative to selected stop and route) implemented on branch `step-6-live-vehicles-on-map`. Vehicle markers draw on the existing OSMDroid map for every entry in `TrackingState.Polling.vehicles`. Marker style: filled circle in the active commute's palette colour, with a triangular notch + rotation when `bearing` is reported. UNCERTAIN positions (`quality != LIVE`, i.e. age > 60 s) render at low alpha with a grey outline (DS.Req.5 / P1-FR10). Camera auto-fits the route polyline + currently-visible vehicles once per active commute session (waits for the polyline cache to be populated rather than fitting to vehicles only). `VehiclePosition` extended with `bearing: Float?`; `GtfsRealtimeRepository` reads `v.position.bearing` when `hasBearing()` is true. `commutePolylines: Map<Int, List<GeoPoint>>` cache populated by `rebuildCommuteOverlays` so the auto-fit can include the route geometry without re-loading the GTFS asset. Build verified clean. Runtime test pending. | All four design choices presented up-front were locked in by the user (coloured dot matching polyline / rotate to reported bearing / grey out uncertain / auto-fit on tracking start). Implementation pattern reused MainActivity's existing palette + index-based colour assignment, so multi-commute support (Step 6 vehicles for the active commute use the *same* colour as the polyline they belong to) was free. Auto-fit is gated by a `lastFittedCommuteKey` so we don't re-zoom every poll cycle — only on transitions into a new active commute window. |
| 2026-05-07 | Step 6 runtime-tested on emulator. Vehicle markers visible and moving on each polling cycle (line 57 → Sofia: 2 vehicles tracking direction_id=1 / Tengdahlsgatan correctly). Bearing reported (73° on the inspected marker). Four UI issues surfaced during the test, all fixed in the same branch in a follow-up commit: BUG-002 (zoom-out cluster — fixed via `MIN_STOP_ZOOM = 14.0` threshold; stops only render after one zoom-in from the default zoom 13), BUG-003 (default OSMDroid pin too large — fixed by replacing the pin with a 6 dp filled blue-grey dot), BUG-010 NEW (vehicle InfoWindow couldn't be dismissed — fixed by adding a `MapEventsOverlay` whose `singleTapConfirmedHelper` calls `InfoWindow.closeAllInfoWindowsOn(map)`), BUG-011 NEW (built-in zoom controls auto-hid and overlapped the live_status banner — fixed by disabling the OSMDroid controller and adding custom always-visible +/- buttons at bottom-end with 72 dp bottom margin; live_status now also hides when state is `Idle`). One observation deferred as BUG-012: vehicle markers often render `UNCERTAIN` even on fresh polls — suspected mismatch between SL's GTFS-RT timestamp semantics (last GPS report) and our 60 s stale threshold; needs empirical tuning during Step 7/10. | The runtime test completed Step 6's functional verification (vehicles drawn, bearing rotation working, auto-fit behaved correctly). The bundle of polish fixes was kept on the same Step 6 branch rather than spun off because they were directly visible during the same test run and are essentially the same step's UX surface — keeping them together preserves a clean "Step 6 was implemented + polished" arc for the thesis. Methodologically: the runtime test surfaced four issues that the build-verify pass alone couldn't have caught (consistent with the Step 4b OOM finding) — keeping runtime tests in the loop continues to be load-bearing. Ready for `/cost` snapshot once the user re-tests the polish bundle. |
| 2026-05-07 | Step 6 polish-bundle runtime-confirmed on a *different* commute than the one that surfaced the issues (line 57 → Hjorthagen from stop Mjärdgränd, `directionCode=1`, picked `direction_id=0 / Motalavägen` correctly via stop-sequence-aware matching). 17 polling cycles over ~7 minutes, 2 vehicles tracked consistently, no fetch failures, heap stable 6–15 MB. BUG-002, BUG-003, BUG-010, BUG-011 all marked **Fixed (runtime confirmed)**. Step 6 (P1-FR5 — Display live position of selected vehicle relative to selected stop and route) marked **Done** in REQUIREMENTS.md. Branch `step-6-live-vehicles-on-map` (3 commits: `9d70326` core, `874239a` polish, `93adedd` runtime-confirmation) ready to merge to main. | **Cost: $11.09 over 1h 28m wall (Checkpoint 5)** — substantially cheaper than Step 5's $30.90, attributable to: zero documentation rabbit hole, one runtime-test iteration to surface UI issues (vs Step 5's four BUG-009 iterations), no asset regeneration, and reuse of Step 5's already-built scaffolding (palette pattern, async polyline cache, lifecycle hooks). Cumulative day total: $65.31 across 5 local sessions ~6h 53m wall. Cumulative bug count: 12 entries in BUGS.md, 9 fixed, 3 deferred (BUG-001 Swedish-character search, BUG-007 30-min commute window cap, BUG-008 polyline overlap rendering, BUG-012 UNCERTAIN threshold). **Two methodological signals for the thesis from this step**: (a) **Cost compresses sub-linearly when scaffolding is reused** — Step 6 added a major feature (live vehicles on map with rotation + auto-fit) plus a 4-bug polish pass for ~36% of Step 5's spend, because Step 5 had already paid for the tracker / state machine / lifecycle / palette / polyline-cache infrastructure. (b) **The "ship → user runtime tests → bundle small UX issues" loop is cheaper than designing up-front against UX issues** — the polish bundle fixed all four issues in one ~150 LOC commit; speculating about which rough edges might exist before any visible artifact would have cost more tokens than the actual fix. |
| 2026-05-07 | Step 7 (P1-FR8 + P1-FR9 + DS.Req.6 — Disruptions / SL Deviations) implemented on branch `step-7-deviations`. Build verified clean. Runtime test pending. **Research-first cycle:** spawned a documentation agent against `https://www.trafiklab.se/api/our-apis/sl/deviations/` and the OpenAPI spec at `static/openapi/sl-deviations.json` in the trafiklab.se site repo (the deviations spec is not in `openApi-docs`, contrary to first guess). Twenty research questions answered with verbatim citations. Headline findings logged in detail in USAGE_LOG entry 080. **Implementation:** new `model/Deviation.kt` + `MessageVariant`; new `repository/SlDeviationsRepository.kt` (OkHttp + Gson, ETag conditional GET — 304 → `FetchResult.NotModified`, 200 → `FetchResult.Modified` with new ETag). Server-side filter: `?line=<id>&site=<id>&future=true`. Client-side filter (`LivePositionTracker.filterByCommuteWindow`): drop deviations whose `publish.upto` is in the past or whose `publish.from` is after the next occurrence of the commute's `timeWindowEnd` — handles cross-midnight windows by rolling end to tomorrow when today's end-time has already passed. Polling: every 60s aligned to every 3rd vehicle tick (`DEVIATION_POLL_RATIO = 3`, `DEFAULT_POLL_INTERVAL_MS = 20_000` already in place from Step 5). Cached deviations + ETag invalidate when active commute's line/site changes. `TrackingState.Polling` extended with `deviations: List<Deviation> = emptyList()` (default for back-compat). **UI:** new top warning bar (`deviation_card` MaterialCardView, amber background `#FFB300`, deep-orange `(!)` accent `#E64A19`) under the search bar in `activity_main.xml`. Hidden when no deviations apply. Highest-importance deviation's Swedish header shown collapsed; `+N` count badge if more than one; tap-to-expand details. Vehicle markers also get a small bottom-right `(!)` badge inside the existing 26 dp dot when any deviation matches the line — rotates with the icon when bearing reported, acceptable since the badge meaning doesn't depend on orientation. **One judgement call flagged for runtime sanity-check:** SL Transport `site.id` / `line.id` ↔ Deviations `scope.stop_areas[].id` / `scope.lines[].id` are STRONG inference shared (same integer namespace, same `transport_authority` field) but NOT a verbatim docs guarantee. The Trafiklab "no shared IDs" caveat from BUG-009 was specific to GTFS Realtime — does NOT apply to Deviations (both are SL integration APIs) — but the docs don't include an explicit "yes these match" sentence. Cheap insurance: log the response on first poll and confirm a known-disrupted line returns non-empty. | First step run with documentation-first as the default approach (the rule added at end of Step 5 in entry 073). Methodological observation for thesis: research agent ran ~3 minutes against the docs and answered all 20 questions with verbatim citations; if the same questions had been answered by guessing-and-iterating the way BUG-009 was approached in Step 5, the cost would likely be 3-5× the research-first cost. The research surfaced two design constraints that would have been easy to miss without docs: (1) deviations have NO direction modeling at all — we can't filter "northbound only", a UI implication that affects how confidently we can warn the user; (2) `priority.importance_level` is "sort-only per docs", with no documented enum or threshold — building "low/medium/high" UI tiers on it would have been heuristic drift like BUG-009 v2's `code-1` mapping. Both of these were preempted by reading the docs first. |
